FROM node:20-bookworm-slim

WORKDIR /app

COPY package.json package-lock.json ./
# The test suite does not use Puppeteer; avoid downloading Chromium during CI installs.
RUN npm ci --ignore-scripts

COPY . .

CMD ["npm", "test"]
